# Side-by-side dock (not a forced default)

Horizontal layout is **user-driven** via drag (see `notes/horizontal-dock-drag.md`), not a factory default.

The brief v5 default (forced horizontal toolkit row) was reverted. `MigrateAwayFromForcedHorizontalV5` only runs when `LayoutVersion == 5`.
