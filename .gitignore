# Build outputs
bin/
obj/
artifacts/
TestResults/
target/
Drawpile/

# .NET user state
*.user
*.suo

# Editor and OS noise
.DS_Store
*.swp
*.log
.idea/
.vscode/

# Local Codex session mount
.codex

# Release secrets (R2 + GitLab token) — see packaging/release/.env.release.example
packaging/release/.env.release

# ML models (downloaded on demand to user data dir)
*.onnx
tmp/