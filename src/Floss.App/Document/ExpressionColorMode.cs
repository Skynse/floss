namespace Floss.App.Document;

public enum ExpressionColorMode
{
    /// <summary>Full color rendering (default).</summary>
    Color,
    /// <summary>Grayscale / luminance only.</summary>
    Gray,
    /// <summary>Black-and-white threshold.</summary>
    Monochrome
}
